package  
{
	import flash.display.Stage;

	/**
	 * HOW TO USE IT:
	 * 
	 * // A top level variable point to flash Stage and accesible from everywhere.
	 * 
	 * // at an instance with access to flash stage:
	 * 
	 * FlashStage = stage
	 * 
	 * // From anywhere:
	 * trace( FlashStage.stageWidth );
	 * 
	 */ 
	
	 
	public var FlashStage : flash.display.Stage
}